{
  "name": "example-part2-b-formularios",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^12.0.0",
    "@testing-library/user-event": "^12.0.0",
    "axios": "^0.27.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "5.0.1",
    "uuid": "^8.3.2",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "server": "json-server -p3001 --watch db.json",
    "build:ui": "rm -rf build && rm -rf /var/www/html/notes-backend/build && npm run build --prod && cp -r build /var/www/html/notes-backend",
    "deploy": "cd /var/www/html/notes-backend && git push heroku main",
    "deploy:full": "npm run build:ui && cd ../notes-backend/build && git add . && git commit -m uibuild && npm run deploy",    
    "logs:prod": "cd /var/www/html/notes-backend && heroku logs --tail"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "json-server": "^0.17.0"
  },
  "proxy": "http://localhost:3002"
}
